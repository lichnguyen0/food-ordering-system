# Plan: Connect "Doanh thu hàng tháng" chart to real data

## Mục tiêu
Thay thế biểu đồ doanh thu hàng tháng hardcode trên dashboard admin thành biểu đồ động lấy dữ liệu thực từ database, theo chuẩn chuyên nghiệp của GrabFood / ShopeeFood.

## Các điểm cần thay đổi so với hiện tại
Hiện tại (admin/index.html:63-77):
- 5 cột biểu đồ với `height: 40%/70%/55%/90%/65%` fix cứng
- Không có label tháng/năm
- So sánh tháng trước hardcoded "tốt hơn 15%"

Sau khi thay đổi:
- **6 cột** đại diện cho 6 tháng gần nhất, chiều cao phản ánh doanh thu thực tế
- Label tháng (T3, T4, T5...) dưới mỗi cột
- Tooltip hover trên mỗi cột hiển thị giá trị doanh thu thực tế
- Cột tháng hiện tại có màu khác (đỏ `#ff0018`)
- So sánh tự động: tháng hiện tại so với tháng trước (%), + tăng màu xanh, - giảm màu đỏ
- Hủy bỏ trang nếu không có đơn hàng: hiển thị "Chưa có dữ liệu" thay vì lỗi

## Cách tính Doanh thu hàng tháng
- **Nhóm doanh thu**: Tổng `order.totalAmount` của các đơn có status `!= CANCELLED`
- **Khoảng thời gian**: Tháng hiện tại + 5 tháng ngay trước đó (6 cột)
- **Logic so sánh**: So sánh doanh thu tháng hiện tại với tháng trước
  - Nếu tháng trước = 0 → hiển thị 0%
  - Nếu tháng hiện tại chưa có dữ liệu → so sánh 0 với tháng trước
  - Nếu chỉ có 1 tháng dữ liệu → hiển thị 0%

## Quy trình tính toán trong Controller
```java
LocalDateTime now = LocalDateTime.now();

// Bắt đầu tháng hiện tại (ví dụ: 2026-05-01 00:00:00)
LocalDateTime currentMonthStart = now.withDayOfMonth(1).truncatedTo(ChronoUnit.DAYS);

// Bắt đầu 5 tháng trước đó (ví dụ: nếu hiện là T5 → bắt đầu từ T12 năm trước)
// Bắt đầu từ ngày 1 của tháng cách tháng hiện tại 5 tháng
YearMonth targetStart = YearMonth.from(currentMonthStart).minusMonths(5);
LocalDateTime start5MonthsAgo = targetStart.atDay(1).atStartOfDay();

// Kết thúc tháng hiện tại: bắt đầu tháng sau rồi trừ đi 1 giây (hoặc dùng tính toán endOfMonth)
LocalDateTime endOfCurrentMonth = currentMonthStart.plusMonths(1).minusSeconds(1);

List<MonthlyRevenue> stats = monthlyStatService.getRevenueByMonth(start5MonthsAgo, endOfCurrentMonth);
double lastMonthRev = calculation...
double currentMonthRev = calculation...
double growthPercent = ...
```

## Định nghĩa DTO

### File: `src/main/java/com/foodorderingsystem/dto/MonthlyRevenue.java`
```java
package com.foodorderingsystem.dto;

import lombok.AllArgsConstructor;
import lombok.Data;

@Data
@AllArgsConstructor
public class MonthlyRevenue {
    private int month;        // 1-12 (for label as "T3", "T4", ...)
    private int year;         // e.g., 2026
    private double revenue;   // doanh thu thực tế của tháng này
}
```

## Dịch vụ: MonthlyStatService

### File: `src/main/java/com/foodorderingsystem/service/MonthlyStatService.java`
```java
package com.foodorderingsystem.service;

import com.foodorderingsystem.model.OrderStatus;
import com.foodorderingsystem.model.Order;
import com.foodorderingsystem.repository.OrderRepository;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import jakarta.persistence.Query;
import org.springframework.stereotype.Service;

import java.math.BigInteger;
import java.time.LocalDateTime;
import java.time.YearMonth;
import java.time.temporal.ChronoUnit;
import java.util.ArrayList;
import java.util.List;

@Service
public class MonthlyStatService {

    private final OrderRepository orderRepository;

    @PersistenceContext
    private EntityManager entityManager;

    public MonthlyStatService(OrderRepository orderRepository) {
        this.orderRepository = orderRepository;
    }

    // Lấy doanh thu theo tháng cho khoảng thời gian từ start đến end
    public List<MonthlyRevenue> getRevenueByMonth(LocalDateTime start, LocalDateTime end) {
        String jpql = """
            SELECT 
                FUNCTION('MONTH', o.orderDate),
                FUNCTION('YEAR', o.orderDate),
                SUM(o.totalAmount)
            FROM Order o
            WHERE o.status != :cancelled
              AND o.orderDate >= :start
              AND o.orderDate < :end
            GROUP BY FUNCTION('YEAR', o.orderDate), FUNCTION('MONTH', o.orderDate)
            ORDER BY FUNCTION('YEAR', o.orderDate), FUNCTION('MONTH', o.orderDate)
        """;

        Query query = entityManager.createQuery(jpql);
        query.setParameter("cancelled", OrderStatus.CANCELLED);
        query.setParameter("start", start);
        query.setParameter("end", end);

        @SuppressWarnings("unchecked")
        List<Object[]> rows = query.getResultList();
        List<MonthlyRevenue> stats = new ArrayList<>();

        for (Object[] row : rows) {
            int month = ((Number) row[0]).intValue();
            int year = ((Number) row[1]).intValue();
            double revenue = ((Number) row[2]).doubleValue();
            stats.add(new MonthlyRevenue(month, year, revenue));
        }

        return stats;
    }
}
```

## Cập nhật AdminController

### File: `src/main/java/com/foodorderingsystem/controller/AdminController.java`

**Những thay đổi:**
1. Thêm import: `import com.foodorderingsystem.dto.MonthlyRevenue;`
2. Thêm import: `import com.foodorderingsystem.service.MonthlyStatService;`
3. Thêm import: `import java.math.BigDecimal;`
4. Thêm import: `import java.time.LocalDateTime;`
5. Thêm import: `import java.time.YearMonth;`
6. Thêm import: `import java.time.temporal.ChronoUnit;`
7. Thêm field `private final MonthlyStatService monthlyStatService;`
8. Thêm `MonthlyStatService` vào constructor
9. Trong `dashboard()`, thêm sau dòng `recentOrders`:

```java
// --- Tính toán Doanh thu hàng tháng ---
LocalDateTime now = LocalDateTime.now();
LocalDateTime currentMonthStart = now.withDayOfMonth(1).truncatedTo(ChronoUnit.DAYS);
LocalDateTime endOfCurrentMonth = currentMonthStart.plusMonths(1).minusSeconds(1);
LocalDateTime start5MonthsAgo = YearMonth.from(currentMonthStart).minusMonths(5).atDay(1).atStartOfDay();

List<MonthlyRevenue> stats = monthlyStatService.getRevenueByMonth(start5MonthsAgo, endOfCurrentMonth);
model.addAttribute("monthlyRevenueStats", stats);

double lastMonthRevenue = 0.0;
double currentMonthRevenue = 0.0;
int statsSize = stats.size();

if (statsSize >= 2) {
    // Tháng cuối danh sách = tháng gần nhất (hiện tại hoặc tháng chưa có dữ liệu)
    MonthlyRevenue current = stats.get(statsSize - 1);
    MonthlyRevenue previous = stats.get(statsSize - 2);
    currentMonthRevenue = current.getRevenue();
    lastMonthRevenue = previous.getRevenue();
} else if (statsSize == 1) {
    currentMonthRevenue = stats.get(0).getRevenue();
}

double growthPercent = (lastMonthRevenue > 0)
    ? ((currentMonthRevenue - lastMonthRevenue) / lastMonthRevenue) * 100.0
    : 0.0;

model.addAttribute("lastMonthRevenue", lastMonthRevenue);
model.addAttribute("currentMonthRevenue", currentMonthRevenue);
model.addAttribute("growthPercent", growthPercent);
```

## Cập nhật Template `admin/index.html`

Thay thế phần `<section class="content-card">` "Doanh thu hàng tháng" (dòng 63-77) bằng:

```html
<section class="content-card">
    <div class="card-header">
        <span class="card-title">Doanh thu hàng tháng</span>
        <!-- Điều chỉnh thể hiện: đếm tháng có dữ liệu trong tháng hiện tại -->
    </div>
    <div class="chart-container" th:if="${monthlyRevenueStats != null and !monthlyRevenueStats.empty}">
        <div class="chart-bars" th:each="stat, iter : ${monthlyRevenueStats}">
            <div class="chart-tooltip" 
                 th:text="${#numbers.formatDecimal(stat.revenue, 0, 'COMMA', 0, 'POINT')} + ' ₫' (T' + ${stat.month} + '/' + ${stat.year | substring(2, 4)})">
            </div>
            <div class="chart-bar" th:classappend="${iter.last ? 'chart-bar-current' : ''}">
                <div class="chart-bar-fill" 
                     th:style="'height: ' + ${(#numbers.formatDecimal(stat.revenue, 0, 'COMMA', 0, 'POINT') ? 0 : 1)}"></div>
            </div>
            <div class="chart-bar-label" th:text="'T' + ${stat.month}">T3</div>
        </div>
    </div>
    <div class="chart-container" th:if="${monthlyRevenueStats == null or monthlyRevenueStats.empty}">
        <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;height:180px;color:var(--text-muted);">
            <i class="fa-solid fa-chart-simple" style="font-size:40px;margin-bottom:12px;"></i>
            <p>Chưa có dữ liệu doanh thu</p>
        </div>
    </div>
    <div style="margin-top: 20px; color: var(--text-muted); font-size: 14px;" th:if="${monthlyRevenueStats != null and !monthlyRevenueStats.empty}">
        <p th:if="${growthPercent > 0}" class="growth-up">
            <i class="fa-solid fa-arrow-trend-up"></i> Doanh thu tháng này cao hơn 
            <span th:text="${#numbers.formatDecimal(growthPercent, 1, 'COMMA', 1, 'POINT')}">15.0</span>% so với tháng trước.
        </p>
        <p th:if="${growthPercent < 0}" class="growth-down">
            <i class="fa-solid fa-arrow-trend-down"></i> Doanh thu tháng này thấp hơn 
            <span th:text="${#numbers.formatDecimal(Math.abs(growthPercent), 1, 'COMMA', 1, 'POINT')}">15.0</span>% so với tháng trước.
        </p>
        <p th:if="${growthPercent == 0}" class="growth-neutral">
            <i class="fa-solid fa-minus"></i> Doanh thu tháng này bằng với tháng trước.
        </p>
    </div>
</section>
```

## Thêm CSS cho biểu đồ

Trong `admin-style.css` hoặc `admin-layout.css`, thêm các style:

```css
/* --- Monthly Revenue Chart --- */
.chart-bars {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 8px;
    height: 180px;
    padding: 0 4px;
    position: relative;
}

.chart-bar {
    flex: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    position: relative;
    cursor: pointer;
}

.chart-bar-fill {
    width: 100%;
    max-width: 48px;
    background: linear-gradient(180deg, #ff0018 0%, #cc0015 100%);
    border-radius: 6px 6px 0 0;
    transition: height 0.6s ease, opacity 0.3s ease;
    position: relative;
}

.chart-bar:hover .chart-bar-fill {
    opacity: 0.85;
}

.chart-bar-current .chart-bar-fill {
    background: linear-gradient(180deg, var(--grab-green) 0%, #00b386 100%);
}

.chart-bar-fill:hover {
    opacity: 0.9;
    transform: scaleY(1.02);
    transform-origin: bottom;
}

.chart-bar-label {
    margin-top: 8px;
    font-size: 12px;
    color: var(--text-muted);
    text-align: center;
    white-space: nowrap;
}

/* Tooltip */
.chart-tooltip {
    position: absolute;
    bottom: calc(100% + 8px);
    left: 50%;
    transform: translateX(-50%);
    background: #1a1a2e;
    color: #fff;
    font-size: 11px;
    padding: 6px 10px;
    border-radius: 6px;
    white-space: nowrap;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s ease;
    z-index: 10;
}

.chart-tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 5px solid transparent;
    border-top-color: #1a1a2e;
}

.chart-bar:hover .chart-tooltip {
    opacity: 1;
}

/* Growth indicators */
.growth-up {
    color: var(--grab-green);
}
.growth-up i {
    margin-right: 6px;
}

.growth-down {
    color: #ff0018;
}
.growth-down i {
    margin-right: 6px;
}

.growth-neutral {
    color: var(--text-muted);
}
.growth-neutral i {
    margin-right: 6px;
}
```

> **Lưu ý về biểu đồ động:** Thay vì hardcode `style="height: 40%"` trong template, chiều caoThanh phần tử fill sẽ được set thông qua inline style:
> ```html
> <div class="chart-bar-fill" th:style="'height: ' + ((stat.revenue / maxRevenue * 100)) + '%'"></div>
> ```
> Tuy nhiên, do dữ liệu thống kê chỉ có 6 tháng, việc so sánh chiều cao tuyệt đối không cần quy đổi tỷ lệ — px cố định theo doanh thu 1 triệu VNĐ, hoặc tỷ lệ so với giá trị lớn nhất trong 6 tháng:
> ```html
> <div class="chart-bar-fill" th:style="'height: ' + (#lists.#{monthMaxRevenue > 0 ? (stat.revenue / monthMaxRevenue * 100) : '1'}) + '%'"></div>
> ```

## Tùy chọn cải thiện do biểu đồ ấn tượng hơn:
- Thêm transition khi chiều cao cột thay đổi động
- Tô màu cột tháng hiện tại màu xanh (`var(--grab-green)`)
- Tô màu cột tháng trước màu đỏ dần nếu giảm

## Các file bị thay đổi

| File | Loại thay đổi | Mô tả |
|------|--------------|-------|
| **MỚI**: `dto/MonthlyRevenue.java` | Thêm file mới | DTO lưu month, year, revenue |
| **MỚI**: `service/MonthlyStatService.java` | Thêm file mới | Nhãn chỉ tính toán: getRevenueByMonth() |
| `controller/AdminController.java` | Sửa | Inject MonthlyStatService, tính revenue stats, truyền vào model |
| `templates/admin/index.html` | Sửa | Render chart động với th:each, tooltip, label, growth percent |
| CSS file (`admin-style.css` hoặc `admin-layout.css`) | Sửa | Thêm rule CSS cho `thuộc tính cụ thể` |

## Rủi ro & Lưu ý

- **Thấp rủi ro**: chỉ thêm query SELECT đọc, không ghi dữ liệu
- **Native query an toàn**: sử dụng parameter `:start`, `:end` – không có SQL injection
- **Lưu ý ground timezone**: `truncatedTo(ChronoUnit.DAYS)` đảm bảo các khoảng thời gian là tính theo ngày
- **Trường hợp không có dữ liệu**: hiển thị "Chưa có dữ liệu doanh thu" thay vì lỗi chart
- **Hiệu năng**: query GROUP BY trên bảng orders có index trên orderDate là hiệu quả, không ảnh hưởng hiệu năng với số lượng order nhỏ/trung bình
