/**
 * Admin Invoice Detail JavaScript
 */

document.addEventListener('DOMContentLoaded', function() {
    // Các chức năng có thể mở rộng sau này (ví dụ tải PDF bằng thư viện html2pdf)
    // Hiện tại chức năng in được gọi trực tiếp bằng window.print() trên nút bấm.
});
